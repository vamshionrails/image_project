apiVersion: v1
kind: Service
metadata:
  name: {{ include "vamshi.fullname" . }}
  labels:
    {{- include "vamshi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vamshi.selectorLabels" . | nindent 4 }}
